--- build_dir/target-mipsel_24kec+dsp2_uClibc-0.9.33.2/linux-ramips_rt3883/linux-3.10.49/drivers/gpio/gpiolib.c 2019-01-29 08:31:13.005843490 -0500
+++ patch_gpiolib.c     2019-01-29 08:36:34.291546681 -0500
@@ -123,7 +123,9 @@
  */
 static struct gpio_desc *gpio_to_desc(unsigned gpio)
 {
-       if (WARN(!gpio_is_valid(gpio), "invalid GPIO %d\n", gpio))
+       if (gpio == -EPROBE_DEFER)
+               return NULL;
+       else if (WARN(!gpio_is_valid(gpio), "invalid GPIO %d\n", gpio))
                return NULL;
        else
                return &gpio_desc[gpio];
